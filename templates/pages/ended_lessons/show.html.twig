{% extends 'base.html.twig' %}

{% block title %}EndedLessons
{% endblock %}

{% block body %}
	<h1>EndedLessons</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ ended_lesson.id }}</td>
			</tr>
			<tr>
				<th>Created_at</th>
				<td>{{ ended_lesson.createdAt ? ended_lesson.createdAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Updated_at</th>
				<td>{{ ended_lesson.updatedAt ? ended_lesson.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_ended_lessons_index') }}">back to list</a>

	<a href="{{ path('app_ended_lessons_edit', {'id': ended_lesson.id}) }}">edit</a>

	{{ include('pages/ended_lessons/_delete_form.html.twig') }}
{% endblock %}
