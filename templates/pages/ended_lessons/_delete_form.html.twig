<form method="post" action="{{ path('app_ended_lessons_delete', {'id': ended_lesson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ended_lesson.id) }}">
    <button class="btn">Delete</button>
</form>
