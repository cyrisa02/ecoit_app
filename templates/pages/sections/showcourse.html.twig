{% extends 'base.html.twig' %}

{% block title %}Sections
{% endblock %}

{% block body %}

	<div class="container mt-4">
		<h1>Sections</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Numéro d'identification</th>
					<td>{{ section.id }}</td>
				</tr>
				<tr>
					<th>Titre</th>
					<td>{{ section.title }}</td>
				</tr>
				<tr>
					<th>Formations</th>
					<td>{{ section.formations}}</td>
				</tr>
				<tr>
					<th>Leçons</th>
					<td>{{ section.lessons}}</td>
				</tr>


			</tbody>
		</table>

		<a class="btn btn-secondary btn-lg" href="{{ path('app_course1') }}">Retour à la liste</a>

	</div>


	{# <a href="{{ path('app_sections_edit', {'id': section.id}) }}">edit</a> #}

	{# {{ include('pages/sections/_delete_form.html.twig') }} #}
{% endblock %}
