{% extends 'base.html.twig' %}

{% block title %}Quiz
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>QUIZ</h1>

		{{ include('pages/quizes/_form.html.twig', {'button_label': 'Valider vos réponses'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_quizes_index') }}">Retour au menu</a>

		{# <a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_questions_new') }}">Créer une nouvelle question</a>
				
				
						{{ include('pages/quizes/_delete_form.html.twig') }}</div> #}

	{% endblock %}
