<form method="post" action="{{ path('app_quizes_delete', {'id': quize.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quize.id) }}">
    <button class="btn">Delete</button>
</form>
